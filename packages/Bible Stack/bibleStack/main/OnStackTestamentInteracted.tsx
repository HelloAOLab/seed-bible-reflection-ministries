/**
    * Handles the interaction event with a testament piece. Calls the method to process a testament interaction.
    *
    * @param {Object} that - An object containing relevant data for the interaction, including the testament piece.
    * @example
    * shout("OnStackTestamentInteracted", {testament: someBot, typeOfInteraction: BibleVizUtils.Data.tags.InteractionType.Tap});
*/

return thisBot.HandleTestamentInteraction(that);