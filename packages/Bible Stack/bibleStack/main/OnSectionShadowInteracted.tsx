/**
 * Handles interaction with a section shadow by delegating the action to the section shadow interaction handler.
 *
 * @param {Object} that - The context object containing interaction details.
 * @example
 * shout("OnSectionShadowInteracted", {sectionShadow: someBot, typeOfInteraction: CanvasInteractions.Tap});
 */

thisBot.HandleSectionShadowInteraction(that);
