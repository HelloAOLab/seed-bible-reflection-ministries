const {mapData} = that

const dimension = os.getCurrentDimension();

shout("switchToYears")
shout("showYears")
mapData.childrenStructureLayer.map(e=>{
    e.map(ee => {
        if (ee.dateWrote.tags.clicked) {
              ee.dateWrote.tags.label = "close"
        } else {
        ee.dateWrote.tags.label = ee.dateWrote.tags.present + " yrs ago"
        }
    })
})