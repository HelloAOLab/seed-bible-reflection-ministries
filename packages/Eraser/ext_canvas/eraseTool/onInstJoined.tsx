// let allBots = getBots("system")

// for(let bot of allBots){
//     if(!bot.tags.system.includes("ab.") && !bot.tags.system.includes("main.eraseTool")){
//         bot.tags.destroyable = true;
//         destroy(bot)
//     }
// }