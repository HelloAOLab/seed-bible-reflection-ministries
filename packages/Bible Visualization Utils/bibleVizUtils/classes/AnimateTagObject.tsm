export class AnimateTagObject
{
    constructor({bot, tag = null, options, then = null})
    {
        this.bot = bot;
        this.tag = tag;
        this.options = options;
        this.then = then;
    }
}