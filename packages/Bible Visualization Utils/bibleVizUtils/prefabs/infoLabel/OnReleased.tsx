/**
    * Resets the tags of the bot when it is released.
    * @example
    * infoLabel.OnReleased();
*/

setTag(thisBot, "transformer", null);
setTag(thisBot, "ownerBotId", null);
setTag(thisBot, "onBotChanged", null);