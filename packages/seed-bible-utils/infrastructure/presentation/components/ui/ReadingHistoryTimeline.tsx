/**
 * Re-export shim — see `../../../../domain/functions/colors` for why this
 * imports the concrete core file. The component's stylesheet moved with it, so
 * importing this still pulls in the timeline CSS as a side effect.
 */
export * from "@packages/seed-bible/seed-bible/components/ReadingHistoryTimeline/ReadingHistoryTimeline";
