/**
 * An environment type which indicates the current "supported" environment.
 * * As this system expands this should become a union of "supported" environments.
 * * E.G. 'casualos' | 'web_preact'
 */
export type Environment = "casualos";
